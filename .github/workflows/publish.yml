name: Publish

on:
  workflow_dispatch:
  push:
    tags:
      - 'agentic-payments-v*'

concurrency: publish-agentic-payments-${{ github.ref }}

jobs:
  publish:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: typescript
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org
          cache: npm
          cache-dependency-path: typescript/package-lock.json

      - run: npm ci
      - run: npm test
      - run: npm publish --access public
