## Summary

<!-- What does this PR do? 1–3 bullets. -->

## Why

<!-- Why is this change needed? Link the issue: Closes #N -->

## Test plan

- [ ] `npm test` passes
- [ ] `npm run check` passes (typecheck + lint + build + publint + attw + size)
- [ ] Changeset included (`npx changeset`) — required if `typescript/src/**` was modified
- [ ] Example smoke test passes (`npm run smoke` from `examples/typescript/`) — required if examples were affected

## Notes

<!-- Anything reviewers should know: edge cases, trade-offs, what was intentionally left out. -->
